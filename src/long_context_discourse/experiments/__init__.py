"""Experiment driver modules.

Each ``exp*.py`` exposes ``run(config)`` and (optionally) ``analyze(config)``.
The CLI in ``scripts_entry`` dispatches to these.
"""

from __future__ import annotations
